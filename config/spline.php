<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Spline 3D (home hero)
    |--------------------------------------------------------------------------
    |
    | Export your scene in Spline → Export → Viewer → copy the .splinecode URL
    | (e.g. https://prod.spline.design/xxxx/scene.splinecode). Leave empty to
    | use the static “bento” hero instead.
    |
    */

    'hero_scene_url' => env('SPLINE_HERO_SCENE_URL', ''),

];
